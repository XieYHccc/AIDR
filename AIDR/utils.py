"""
Standalone functions which require only imports from numpy and the
standard library.

Other libraries may be imported must be wrapped in try/except blocks
or imported inside a function
"""

import types
import operator
import numbers

import numpy as np
